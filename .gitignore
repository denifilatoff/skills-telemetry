
# APM dependencies
apm_modules/

# Local Codex and agent state
.agents/
.claude/
.codex/
.cursor/

# Local APM workbench install (machine-specific paths; never committed)
apm.yml
apm.lock.yaml

# Sender build output
cli/dist/
cli/skills-telemetry
cli/skills-telemetry.exe

# Skill eval runs (local only)
eval-workspace/
