
# APM dependencies
apm_modules/

# Local Codex and agent state (all APM `apm compile` output)
.agents/
.claude/
.codex/
.cursor/
.kiro/
.windsurf/
.github/instructions/
/AGENTS.md
/GEMINI.md

# Local APM workbench install (machine-specific paths; never committed)
apm.yml
apm.lock.yaml

# CLI build output
dist/
/skills-telemetry
/skills-telemetry.exe

# Skill eval runs (local only)
eval-workspace/

# Local backend deploy (test only)
local-deploy-otel/

# Ready-made telemetry config for install testing (endpoint + token; never committed)
/test-telemetry.env
